import React from "react";
import classNames from "classnames/bind";
import styles from "./page.module.scss";

const cx = classNames.bind(styles);

type Props = {};

const Mint = function ({}: Props) {
    return <main>mint</main>;
};

export default Mint;
