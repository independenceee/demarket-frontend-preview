import React from "react";
import Navbar from "../Navbar";

type Props = {};
const Header = function ({}: Props) {
    return (
        <header>
            <Navbar />
        </header>
    );
};

export default Header;
